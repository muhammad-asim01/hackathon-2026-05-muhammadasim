name: Deploy Frontend → Vercel (Production)

# ── Trigger ───────────────────────────────────────────────────────────────────
# Runs ONLY when code lands on main (direct push or merged PR).
# All other branches are ignored.
on:
  push:
    branches:
      - main
    paths:
      - "main-project/frontend/**"
      - ".github/workflows/deploy-frontend.yml"

# ── Permissions ───────────────────────────────────────────────────────────────
permissions:
  contents: read
  deployments: write

# ── Jobs ─────────────────────────────────────────────────────────────────────
jobs:
  deploy:
    name: Build & Deploy to Vercel
    runs-on: ubuntu-latest
    environment: production

    defaults:
      run:
        working-directory: main-project/frontend

    steps:
      # ── 1. Checkout ──────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. Setup Node ────────────────────────────────────────────────────────
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: main-project/frontend/package-lock.json

      # ── 3. Install dependencies ──────────────────────────────────────────────
      - name: Install dependencies
        run: npm ci

      # ── 4. Lint ──────────────────────────────────────────────────────────────
      - name: Run ESLint
        run: npm run lint

      # ── 5. Type-check ────────────────────────────────────────────────────────
      - name: TypeScript type-check
        run: npx tsc --noEmit

      # ── 6. Install Vercel CLI ────────────────────────────────────────────────
      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      # ── 7. Pull project config from Vercel ───────────────────────────────────
      # Downloads .vercel/project.json so the CLI knows which project to target.
      # We inject env vars directly from GitHub Secrets in step 8 instead of
      # relying on the Vercel dashboard — giving full control in one place.
      - name: Pull Vercel project config
        run: |
          vercel pull \
            --yes \
            --environment=production \
            --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      # ── 8. Write .env.production.local (build-time vars) ─────────────────────
      # NEXT_PUBLIC_* vars must be present at build time — baked into JS bundle.
      - name: Write production env file
        run: printf '%s\n' "$PRODUCTION_ENV" > .env.production.local
        env:
          PRODUCTION_ENV: ${{ vars.PRODUCTION }}

      # ── 9. Sync env vars to Vercel dashboard (runtime vars) ───────────────────
      # Server-side vars (NEXTAUTH_SECRET, GOOGLE_CLIENT_*, etc.) are read by
      # Vercel's serverless functions at runtime — not from the build artifact.
      # This step pushes every KEY=VALUE from GitHub → Vercel automatically,
      # so you only ever update one place (GitHub) and Vercel stays in sync.
      - name: Sync env vars to Vercel dashboard
        run: |
          # Parse each KEY=VALUE line and upsert it into Vercel production env.
          # --force overwrites if the var already exists — safe to run every time.
          while IFS='=' read -r key value; do
            # Skip blank lines and comment lines
            [[ -z "$key" || "$key" == \#* ]] && continue
            echo "Syncing: $key"
            printf '%s' "$value" | vercel env add "$key" production \
              --token=${{ secrets.VERCEL_TOKEN }} \
              --force 2>&1 || true
          done <<< "$PRODUCTION_ENV"
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          PRODUCTION_ENV: ${{ vars.PRODUCTION }}

      # ── 10. Build ─────────────────────────────────────────────────────────────
      # `vercel build --prod` uses the .env.production.local written in step 8.
      # If the build fails the whole workflow stops — deploy never runs.
      - name: Build with Vercel (production)
        run: |
          vercel build \
            --prod \
            --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      # ── 11. Deploy prebuilt output ───────────────────────────────────────────
      # --prebuilt ships exactly what step 9 verified — Vercel does not rebuild.
      # Captures the live URL and writes it to GITHUB_OUTPUT for the summary.
      - name: Deploy to Vercel (production)
        id: deploy
        run: |
          DEPLOY_URL=$(vercel deploy \
            --prebuilt \
            --prod \
            --token=${{ secrets.VERCEL_TOKEN }})
          echo "deploy_url=$DEPLOY_URL" >> "$GITHUB_OUTPUT"
          echo "Deployed → $DEPLOY_URL"
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      # ── 12. Post-deploy summary ──────────────────────────────────────────────
      - name: Write job summary
        if: always()
        run: |
          if [ "${{ steps.deploy.outcome }}" = "success" ]; then
            echo "## ✅ Deployment successful" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "| Field | Value |" >> "$GITHUB_STEP_SUMMARY"
            echo "|---|---|" >> "$GITHUB_STEP_SUMMARY"
            echo "| URL | ${{ steps.deploy.outputs.deploy_url }} |" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "## ❌ Deployment failed" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "Check the logs above for the failing step." >> "$GITHUB_STEP_SUMMARY"
          fi
          echo "| Branch | \`main\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Commit | \`${{ github.sha }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Triggered by | @${{ github.actor }} |" >> "$GITHUB_STEP_SUMMARY"
